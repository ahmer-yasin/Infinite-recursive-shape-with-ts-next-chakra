import {
  ThemeProvider,
} from '@chakra-ui/core';
import Shape from '../src/modules/shape/Shape';
const IndexPage = () => (
  <ThemeProvider>
    <Shape/>
  </ThemeProvider>
)

export default IndexPage
